part of 'values.dart';

abstract class AppPadding {
  static const signInButton = EdgeInsets.all(8.0);
  static const dismissible = EdgeInsets.all(16.0);
  static const jokeScreen = EdgeInsets.all(15.0);
  static const jokeDismissible = EdgeInsets.symmetric(vertical: 5.0, horizontal: 16.0);
  static const scoreBoard = EdgeInsets.symmetric(vertical: 1.0, horizontal: 10.0);
}
